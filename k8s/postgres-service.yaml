apiVersion: v1
kind: Service
metadata:
  name: ecommerce-postgres-service
spec:
  selector:
    app: ecommerce-postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP